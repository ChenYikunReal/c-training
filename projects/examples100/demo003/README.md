# 思路分析
```text
假设该数为 x。
则：x + 100 = n2, x + 100 + 168 = m2
计算等式：m2 - n2 = (m + n)(m - n) = 168
令 m + n = i，m - n = j，i * j =168，i 和 j 至少一个是偶数
可得： m = (i + j) / 2， n = (i - j) / 2，i 和 j 要么都是偶数，要么都是奇数。
由上可知，i 与 j 均是大于等于 2 的偶数。
由于 i * j = 168， j>=2，则 1 < i < 168 / 2 + 1。
接下来将 i 的所有数字循环计算即可。
```
